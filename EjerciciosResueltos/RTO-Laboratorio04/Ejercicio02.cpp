#include <QCoreApplication>
#include <QDebug>
#include <iostream>
#include <vector>
#include <algorithm>
#include <iostream>

using namespace std;
/*
int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);

    int u = 5;

    // Función lambda para sumar un número y mostrarlo
    auto s = [](int valor) { return valor + 3; };
    qDebug() << "Resultado de la suma:" << s(u);  // Suma u + 3 y muestra el resultado

    // Función lambda para recorrer los elementos de un vector
    vector<int> v {5, 2, 3, 7, 1, 0, 14, 20};
    qDebug() << "Elementos del vector:";
    for_each(v.begin(), v.end(), [](int valor) {
        qDebug() << valor;
    });

    return a.exec();
}
*/
